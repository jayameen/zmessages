spring:
  application:
    name: zmessages
  servlet:
    multipart:
      max-file-size: 10000000
      max-request-size: 10000000

server:
  port: 8086
  servlet:
    context-path: /${spring.application.name}

---
spring:
  config:
    activate:
      on-profile: default

app:
  smtp:
    gmail:
      host: smtp.gmail.com
      port: 587
      username: ${GMAIL_USERNAME:madan@xindus.net}
      password: ${GMAIL_PASSWORD:admin123}
    debug: true

